<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="socialUp.service.common.mapper">
<!-- SQL_CALC_FOUND_ROWS 을 사용해서 조건에 맞는  모든 카운트를 알고싶을때 -->
	<select id="selectFoundRows" resultType="Long" >
    	SELECT FOUND_ROWS() as totalRowCount
	</select>
</mapper>

